import { typographyBaseTheme, typographyDarkTheme } from '../typographyTheme';

describe('typographyBaseTheme', () => {
  it('should be defined', () => {
    expect(typographyBaseTheme).toBeDefined();
  });
});

describe('typographyDarkTheme', () => {
  it('should be defined', () => {
    expect(typographyDarkTheme).toBeDefined();
  });
});
