import { boxBaseTheme, boxDarkTheme } from '../boxTheme';

describe('boxBaseTheme', () => {
  it('should be defined', () => {
    expect(boxBaseTheme).toBeDefined();
  });
});

describe('boxDarkTheme', () => {
  it('should be defined', () => {
    expect(boxDarkTheme).toBeDefined();
  });
});
